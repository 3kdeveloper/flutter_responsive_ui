import 'package:flutter/material.dart';

class ResponsiveScalerView extends StatelessWidget {
  const ResponsiveScalerView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Responsive Scaler')));
  }
}
